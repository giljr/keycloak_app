<h1>User Access Granted</h1>
<p>You are authorized as an <strong>user</strong>.</p>

<%= link_to "🚪 Log out", 
      "#{ENV['KEYCLOAK_SITE']}/realms/#{ENV['KEYCLOAK_REALM']}/protocol/openid-connect/logout?redirect_uri=#{@logout_jwt}", 
      class: "btn btn-danger" %>

<p><a href="/"> 🏠️ Return home </a></p>

<h2>Decoded Token Payload</h2>
<pre><%= JSON.pretty_generate(@user_info) %></pre>
